@page
@model Autorep.Web.Pages.Admin.Companies.NewModel
@{
    ViewData["Title"] = "New company";
}

<div class="page-header">
    <div class="page-header__heading">
        <h1>New testing company</h1>
    </div>
</div>

<div class="card" style="max-width:560px">
    <form method="post">
        <div class="form-field">
            <label asp-for="Input.Name">Name</label>
            <input asp-for="Input.Name" required autofocus />
        </div>
        @if (Model.Errors.Any())
        {
            <div class="alert alert--danger">
                <ul>@foreach (var e in Model.Errors) { <li>@e</li> }</ul>
            </div>
        }
        <div class="form-actions">
            <button type="submit" class="btn">Create</button>
            <a class="btn btn--secondary" asp-page="/Admin/Companies/Index">Cancel</a>
        </div>
    </form>
</div>
