{
  "name": "autorep-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AutoRep PWA client: shared TypeScript modules (Wizard Step Resolver, Pass/Fail Calculator, Sync Client) + the offline Preact wizard backed by IndexedDB. Bundled with esbuild, tested with Vitest.",
  "scripts": {
    "build": "esbuild Client/main.ts --bundle --splitting --format=esm --jsx=automatic --jsx-import-source=preact --outdir=wwwroot/js/dist --entry-names=autorep --chunk-names=chunks/[name]-[hash]",
    "build:prod": "esbuild Client/main.ts --bundle --minify --splitting --format=esm --jsx=automatic --jsx-import-source=preact --outdir=wwwroot/js/dist --entry-names=autorep --chunk-names=chunks/[name]-[hash]",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "idb": "^8.0.0",
    "pdf-lib": "^1.17.1",
    "pdfmake": "^0.3.10",
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pdfmake": "^0.3.3",
    "esbuild": "^0.25.0",
    "fake-indexeddb": "^6.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
